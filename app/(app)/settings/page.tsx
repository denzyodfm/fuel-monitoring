import {requireSession} from "@/lib/auth";import {PageHeading} from "@/components/page-heading";export default async function Page(){await requireSession(["ADMINISTRATOR"]);return <><PageHeading title="Settings" description="Organization-wide application behavior."/><div className="card max-w-2xl grid gap-4"><label>Application timezone<input className="field" value="Asia/Manila" readOnly/></label><label>Currency<input className="field" value="PHP — Philippine Peso" readOnly/></label><label>Upload storage<input className="field" value="Local filesystem abstraction (UPLOAD_DIR)" readOnly/></label><p className="text-sm text-slate-500">Operational settings are stored as JSON in AppSetting and can be migrated to a dedicated settings service without schema changes.</p></div></>}
