import {db} from "@/lib/db";import {requireSession} from "@/lib/auth";import {php,liters} from "@/lib/format";import {PageHeading} from "@/components/page-heading";
export default async function Page(){const s=await requireSession();const monthly=await db.$queryRaw<{period:string;company:string;liters:unknown;amount:unknown;requested:unknown}[]>`SELECT DATE_FORMAT(t.transactionDate,'%Y-%m') period,c.code company,SUM(t.liters) liters,SUM(t.amount) amount,SUM(COALESCE(t.requestedAmount,0)) requested FROM FuelTransaction t LEFT JOIN Company c ON c.id=t.companyId WHERE t.organizationId=${s.organizationId} AND t.deletedAt IS NULL GROUP BY period,c.code ORDER BY period DESC,amount DESC`;return <><PageHeading title="Reports" description="Normalized monthly and semi-monthly business reporting."/><div className="grid md:grid-cols-3 gap-4 mb-6">{["Monthly fuel summary","Semi-monthly (1–15 / 16–end)","Usage by company & vehicle","Price-per-liter trend","Requested vs actual","Personal-use transactions","Vehicle fuel efficiency","Insurance expiration","AP / chargeback summary","Data-quality exceptions"].map(x=><div className="card" key={x}><strong>{x}</strong><p className="text-sm text-slate-500">Filterable database query · CSV/Excel-ready</p></div>)}</div><div className="card desktop-table"><h2 className="font-bold">Monthly summary</h2><table className="table"><thead><tr><th>Month</th><th>Company</th><th>Liters</th><th>Actual</th><th>Requested</th><th>Variance</th></tr></thead><tbody>{monthly.map((r,i)=><tr key={i}><td>{r.period}</td><td>{r.company??"Unmapped"}</td><td>{liters(String(r.liters))}</td><td>{php.format(Number(r.amount))}</td><td>{php.format(Number(r.requested))}</td><td>{php.format(Number(r.requested)-Number(r.amount))}</td></tr>)}</tbody></table>{!monthly.length&&<p className="text-slate-500 p-6">Import transactions to populate reports.</p>}</div></>}
