DATABASE_URL="mysql://fuel:fuel_password@localhost:3306/fuel_monitoring"
AUTH_SECRET="replace-with-at-least-32-random-characters"
NEXT_PUBLIC_APP_URL="http://localhost:3010"
# Set to true only when the app is served over HTTPS. Over plain HTTP the browser
# discards a Secure session cookie and every navigation looks like a logout.
COOKIE_SECURE="false"
UPLOAD_DIR="./uploads"
IMPORT_WORKBOOK_PATH="./data/Fuel Monitoring 2026 FROM BERN 8.12.26.xlsx"
