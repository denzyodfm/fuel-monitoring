import {chromium} from "@playwright/test";import fs from "node:fs";
const executablePath=["C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe","C:/Program Files/Microsoft/Edge/Application/msedge.exe"].find(path=>fs.existsSync(path));const browser=await chromium.launch({executablePath,headless:true});const page=await browser.newPage();await page.goto("http://127.0.0.1:3000/login");await page.getByLabel("Email").fill("admin@fuel.local");await page.getByLabel("Password").fill("FuelAdmin2026!");await page.getByRole("button",{name:"Sign in"}).click();await page.waitForURL("**/dashboard");await page.goto("http://127.0.0.1:3000/transactions",{waitUntil:"networkidle"});await page.getByRole("button",{name:"Edit"}).first().click();const prefilled=await page.getByRole("dialog").getByLabel("Liters").inputValue();await page.getByRole("dialog").getByRole("button",{name:"Cancel"}).click();const closedAfterCancel=await page.getByRole("dialog").count()===0;await page.getByRole("button",{name:"Edit"}).first().click();await page.getByRole("dialog").getByRole("button",{name:"Save changes"}).click();await page.getByRole("dialog").waitFor({state:"detached",timeout:30000});console.log(JSON.stringify({prefilledLiters:prefilled,closedAfterCancel,closedAfterSave:await page.getByRole("dialog").count()===0},null,2));await browser.close();
