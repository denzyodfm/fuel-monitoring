import {chromium} from "@playwright/test";import fs from "node:fs";
const executablePath=["C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe","C:/Program Files/Microsoft/Edge/Application/msedge.exe"].find(path=>fs.existsSync(path));const browser=await chromium.launch({executablePath,headless:true});const page=await browser.newPage();await page.goto("http://127.0.0.1:3000/login");await page.getByLabel("Email").fill("admin@fuel.local");await page.getByLabel("Password").fill("FuelAdmin2026!");await page.getByRole("button",{name:"Sign in"}).click();await page.waitForURL("**/dashboard");await page.goto("http://127.0.0.1:3000/transactions",{waitUntil:"networkidle"});await page.getByLabel("Search").fill("TOYOTA");await page.locator(".suggestions button").first().waitFor();const suggestionCount=await page.locator(".suggestions button").count();const exportResponse=await page.request.get("http://127.0.0.1:3000/api/transactions/export?status=APPROVED");console.log(JSON.stringify({url:page.url(),heading:await page.getByRole("heading",{name:"Fuel transactions"}).textContent(),suggestionCount,printButton:await page.getByRole("button",{name:"Print"}).isVisible(),downloadButton:await page.getByRole("button",{name:"Download Excel"}).isVisible(),exportStatus:exportResponse.status(),exportType:exportResponse.headers()["content-type"],rows:await page.locator("tbody tr").count()},null,2));await browser.close();
