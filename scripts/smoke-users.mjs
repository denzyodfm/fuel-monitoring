import {chromium} from "@playwright/test";import fs from "node:fs";const executablePath=["C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe","C:/Program Files/Microsoft/Edge/Application/msedge.exe"].find(x=>fs.existsSync(x));const browser=await chromium.launch({executablePath,headless:true});const page=await browser.newPage();await page.goto("http://127.0.0.1:3000/login",{timeout:60000});await page.getByLabel("Email").fill("admin@fuel.local");await page.getByLabel("Password").fill("FuelAdmin2026!");await page.getByRole("button",{name:"Sign in"}).click();await page.waitForURL("**/dashboard",{timeout:60000});await page.goto("http://127.0.0.1:3000/users",{waitUntil:"networkidle",timeout:60000});const ownRow=page.locator("tbody tr").filter({hasText:"admin@fuel.local"});const ownDeleteDisabled=await ownRow.getByRole("button",{name:"Delete"}).isDisabled();await page.getByRole("button",{name:"Add user"}).click();const dialog=page.getByRole("dialog");console.log(JSON.stringify({dialog:await dialog.isVisible(),roles:await dialog.getByLabel("Role").locator("option").allTextContents(),passwordRequired:await dialog.getByLabel("Password").getAttribute("required")!==null,ownDeleteDisabled},null,2));await browser.close();
