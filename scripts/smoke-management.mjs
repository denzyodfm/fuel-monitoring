import {chromium} from "@playwright/test";import fs from "node:fs";
const executablePath=["C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe","C:/Program Files/Microsoft/Edge/Application/msedge.exe"].find(x=>fs.existsSync(x));const browser=await chromium.launch({executablePath,headless:true});const page=await browser.newPage();
await page.goto("http://127.0.0.1:3000/login");await page.getByLabel("Email").fill("admin@fuel.local");await page.getByLabel("Password").fill("FuelAdmin2026!");await page.getByRole("button",{name:"Sign in"}).click();await page.waitForURL("**/dashboard");
await page.goto("http://127.0.0.1:3000/vehicles",{waitUntil:"networkidle"});await page.getByRole("button",{name:"Add vehicle"}).click();let modal=page.locator(".modal-card");const companyOptions=await modal.getByLabel("Company").locator("option").count();await modal.getByRole("button",{name:"Cancel"}).click();await page.getByRole("button",{name:"Delete"}).first().click();modal=page.locator(".modal-card");const deletePrompt=await modal.getByText("Type DELETE to confirm").isVisible();await modal.getByRole("button",{name:"Cancel"}).click();
await page.goto("http://127.0.0.1:3000/transactions",{waitUntil:"networkidle"});await page.getByRole("button",{name:"Add transaction"}).click();const addDialog=await page.getByRole("dialog").isVisible();const saveVisible=await page.getByRole("button",{name:"Save transaction"}).isVisible();console.log(JSON.stringify({companyOptions,deletePrompt,addDialog,saveVisible},null,2));await browser.close();
