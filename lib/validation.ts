import { z } from "zod";
export const transactionSchema=z.object({transactionDate:z.coerce.date(),invoiceNumber:z.string().trim().optional(),poNumber:z.string().trim().optional(),operatorId:z.string().optional(),companyId:z.string().min(1),vehicleId:z.string().optional(),purposeId:z.string().optional(),unitPrice:z.coerce.number().positive(),liters:z.coerce.number().positive(),requestedAmount:z.coerce.number().nonnegative().optional(),odometer:z.coerce.number().nonnegative().optional(),notes:z.string().max(2000).optional(),overrideReason:z.string().optional()}).superRefine((v,c)=>{if(v.overrideReason!==undefined&&v.overrideReason.trim().length<5)c.addIssue({code:"custom",path:["overrideReason"],message:"Override reason must be meaningful"})});
