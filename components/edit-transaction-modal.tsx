"use client";
import {useEffect,useState,useTransition} from "react";
import {updateTransaction} from "@/app/(app)/transactions/actions";
type Option={value:string;label:string};
type Transaction={id:string;transactionDate:string;invoiceNumber:string;poNumber:string;companyId:string;vehicleId:string;operatorId:string;purposeId:string;unitPrice:string;liters:string;requestedAmount:string;odometer:string;notes:string};
export function EditTransactionModal({transaction,companies,vehicles,operators,purposes}:{transaction:Transaction;companies:Option[];vehicles:Option[];operators:Option[];purposes:Option[]}){const [open,setOpen]=useState(false);const [error,setError]=useState("");const [pending,startTransition]=useTransition();useEffect(()=>{if(!open)return;const close=(event:KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};window.addEventListener("keydown",close);return()=>window.removeEventListener("keydown",close)},[open]);function submit(formData:FormData){setError("");startTransition(async()=>{const result=await updateTransaction(transaction.id,formData);if(result.ok)setOpen(false);else setError(result.error)})}return <><button className="btn secondary" type="button" onClick={()=>setOpen(true)}>Edit</button>{open&&<div className="modal-backdrop" role="presentation" onMouseDown={event=>{if(event.target===event.currentTarget)setOpen(false)}}><section className="modal-card" role="dialog" aria-modal="true" aria-labelledby={`edit-${transaction.id}`}><div className="modal-heading"><div><h2 id={`edit-${transaction.id}`}>Edit fuel transaction</h2><p>Update the transaction details below. Amount is recalculated automatically.</p></div><button type="button" className="modal-close" aria-label="Close" onClick={()=>setOpen(false)}>×</button></div><form action={submit} className="edit-grid">{error&&<p className="form-error">{error}</p>}<label>Date<input className="field" type="date" name="transactionDate" defaultValue={transaction.transactionDate} required/></label><label>Company<select className="field" name="companyId" defaultValue={transaction.companyId} required><option value="">Select company</option>{companies.map(item=><option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label>P.O. number<input className="field" name="poNumber" defaultValue={transaction.poNumber}/></label><label>Invoice number<input className="field" name="invoiceNumber" defaultValue={transaction.invoiceNumber}/></label><label>Vehicle<select className="field" name="vehicleId" defaultValue={transaction.vehicleId}><option value="">No vehicle</option>{vehicles.map(item=><option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label>Driver / operator<select className="field" name="operatorId" defaultValue={transaction.operatorId}><option value="">No operator</option>{operators.map(item=><option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label>Purpose<select className="field" name="purposeId" defaultValue={transaction.purposeId}><option value="">No purpose</option>{purposes.map(item=><option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label>Odometer<input className="field" name="odometer" type="number" min="0" step="0.01" defaultValue={transaction.odometer}/></label><label>Unit price<input className="field" name="unitPrice" type="number" min="0.0001" step="0.0001" defaultValue={transaction.unitPrice} required/></label><label>Liters<input className="field" name="liters" type="number" min="0.001" step="0.001" defaultValue={transaction.liters} required/></label><label>Requested amount<input className="field" name="requestedAmount" type="number" min="0" step="0.01" defaultValue={transaction.requestedAmount}/></label><label className="edit-wide">Notes<textarea className="field" name="notes" defaultValue={transaction.notes}/></label><div className="modal-actions"><button type="button" className="btn secondary" onClick={()=>setOpen(false)} disabled={pending}>Cancel</button><button className="btn" disabled={pending}>{pending?"Saving…":"Save changes"}</button></div></form></section></div>}</>}
