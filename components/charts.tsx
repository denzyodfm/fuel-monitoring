"use client";import {Area,AreaChart,Bar,BarChart,CartesianGrid,LabelList,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis} from "recharts";
import {liters as fmtLiters,php} from "@/lib/format";
const axisTick=(value:number)=>Number(value).toLocaleString("en-PH");
const tooltipValue=(value:unknown,name:unknown)=>String(name)==="liters"?fmtLiters(Number(value)):php.format(Number(value));
export function TrendChart({data}:{data:{month:string;expense:number;liters:number}[]}){return <ResponsiveContainer width="100%" height={280}><AreaChart data={data}><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#dc2626" stopOpacity={.32}/><stop offset="95%" stopColor="#f59e0b" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis width={78} tickFormatter={axisTick}/><Tooltip formatter={tooltipValue}/><Legend/><Area dataKey="expense" stroke="#b91c1c" fill="url(#g)"/><Area dataKey="liters" stroke="#d97706" fill="transparent"/></AreaChart></ResponsiveContainer>}
export function CompanyChart({data}:{data:{name:string;expense:number}[]}){return <ResponsiveContainer width="100%" height={280}><BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis width={78} tickFormatter={axisTick}/><Tooltip formatter={tooltipValue}/><Bar dataKey="expense" fill="#b91c1c" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer>}
const pesoTick=(value:number)=>Number(value).toLocaleString("en-PH",{minimumFractionDigits:2,maximumFractionDigits:2});
// Mirrors the printed "Fuel Consumption of Company Vehicles Monitoring" chart: one bar per month, amount in pesos.
export function MonthlyAmountChart({data}:{data:{month:string;Amount:number}[]}){return <ResponsiveContainer width="100%" height={380}><BarChart data={data} margin={{top:24,right:12,left:12}}><CartesianGrid vertical={false} stroke="#e2e8f0"/><XAxis dataKey="month" tickLine={false}/><YAxis width={96} tickFormatter={pesoTick} axisLine={false} tickLine={false}/><Tooltip formatter={(value:unknown)=>php.format(Number(value))}/><Legend/><Bar dataKey="Amount" fill="#c0504d" maxBarSize={110} isAnimationActive={false}><LabelList dataKey="Amount" position="top" formatter={(value:unknown)=>pesoTick(Number(value))} style={{fontSize:12,fill:"#334155"}}/></Bar></BarChart></ResponsiveContainer>}
